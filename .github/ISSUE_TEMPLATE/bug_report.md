---
name: Bug report
about: Something behaves differently than it should
labels: bug
---

**What happened**

**What you expected**

**Setup**
- OS and version:
- Python version (`python3 -V`):
- Web server (nginx / other / none):
- Databases in use (MySQL / PostgreSQL / SQLite / none):
- Installed via curl or from a clone:

**Output**

```
systemctl status srvmon srvmon-collector
journalctl -u srvmon-collector -n 50
```

**Screenshot**, if the problem is on a page. Turn on demo mode first if the
page shows anything you would rather not publish — see the README.
